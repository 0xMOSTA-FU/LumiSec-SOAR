{
  "name": "soar-backend",
  "version": "1.0.0",
  "description": "External Node.js + MongoDB backend for the SOAR platform. Provides extended REST APIs (incidents, assets, threat intel, SOAR event ingestion) that the main Next.js app consumes via lib/external-api.ts.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "mongoose": "^8.9.5",
    "morgan": "^1.10.0",
    "node-cron": "^3.0.3",
    "uuid": "^11.0.5"
  },
  "engines": {
    "node": ">=20"
  }
}
